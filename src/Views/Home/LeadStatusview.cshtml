@{

	// View
	View = this;
	SetClientVar("login", LoginStatus);

	// Global Page Rendering event
	Page_Rendering();

	// Page Render event
	LeadStatus_View.Page_Render();

	// Check action result
	if (LeadStatus_View.ActionResult is EmptyResult) { // Terminate page
		return;
	}

	// Layout
	Layout = "_Layout";

	// Local variables (Temp data)
	#pragma warning disable 168
	string sqlWrk, filterWrk, whereWrk, curVal;
	List<Dictionary<string, object>> rswrk;
	string[] arWrk;
	List<object> listWrk;
	Func<string> lookupFilter;
	#pragma warning restore 168
}
@if (!LeadStatus_View.IsExport()) {
<script>
var fLeadStatusview, currentPageID;
loadjs.ready("head", function() {

	// Form object
	currentPageID = ew.PAGE_ID = "view";
	fLeadStatusview = currentForm = new ew.Form("fLeadStatusview", "view");
	loadjs.done("fLeadStatusview");
});
</script>
<script>
loadjs.ready("head", function() {

	// Client script
	// Write your client script here, no need to add script tags.

});
</script>
}
@if (!LeadStatus_View.IsExport()) {
<div class="btn-toolbar ew-toolbar">
	@LeadStatus_View.ExportOptions.RenderBody()
	@LeadStatus_View.OtherOptions.RenderBody()
	<div class="clearfix"></div>
</div>
}
@LeadStatus_View.ShowPageHeader()
@LeadStatus_View.ShowMessages()
<form name="fLeadStatusview" id="fLeadStatusview" class="form-inline ew-form ew-view-form" method="post">
@if (LeadStatus_View.CheckToken) {
	@Html.AntiForgeryToken()
}
<input type="hidden" name="modal" value="@ConvertToInt(LeadStatus_View.IsModal)">
<table class="table table-striped table-sm ew-view-table">
@if (LeadStatus_View.Id.Visible) { // LeadStatus_View.Id.Visible (begin)
	<tr id="r_Id">
		<td class="@(LeadStatus_View.TableLeftColumnClass)"><span id="elh_LeadStatus_Id">@Html.Raw(LeadStatus_View.Id.Caption)</span></td>
		<td data-name="Id" @Html.Raw(LeadStatus_View.Id.CellAttributes)>
<span id="el_LeadStatus_Id">
<span @Html.Raw(LeadStatus_View.Id.ViewAttributes)>@Html.Raw(LeadStatus_View.Id.GetViewValue())</span>
</span>
</td>
	</tr>
} @* LeadStatus_View.Id.Visible (end) *@
@if (LeadStatus_View.DisplayName.Visible) { // LeadStatus_View.DisplayName.Visible (begin)
	<tr id="r_DisplayName">
		<td class="@(LeadStatus_View.TableLeftColumnClass)"><span id="elh_LeadStatus_DisplayName">@Html.Raw(LeadStatus_View.DisplayName.Caption)</span></td>
		<td data-name="DisplayName" @Html.Raw(LeadStatus_View.DisplayName.CellAttributes)>
<span id="el_LeadStatus_DisplayName">
<span @Html.Raw(LeadStatus_View.DisplayName.ViewAttributes)>@Html.Raw(LeadStatus_View.DisplayName.GetViewValue())</span>
</span>
</td>
	</tr>
} @* LeadStatus_View.DisplayName.Visible (end) *@
</table>
@{ LeadStatus_View.CloseRecordset(); }
</form>
@LeadStatus_View.ShowPageFooter()
@ShowDebugMessage()
@ElapsedTime()
@if (!LeadStatus_View.IsExport()) {
<script>
loadjs.ready("load", function() {

	// Startup script
	// Write your table-specific startup script here
	// document.write("page loaded");

});
</script>
}