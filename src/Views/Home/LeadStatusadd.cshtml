@{

	// View
	View = this;
	SetClientVar("login", LoginStatus);

	// Global Page Rendering event
	Page_Rendering();

	// Page Render event
	LeadStatus_Add.Page_Render();

	// Check action result
	if (LeadStatus_Add.ActionResult is EmptyResult) { // Terminate page
		return;
	}

	// Layout
	Layout = "_Layout";

	// Local variables (Temp data)
	#pragma warning disable 168
	string sqlWrk, filterWrk, whereWrk, curVal;
	List<Dictionary<string, object>> rswrk;
	string[] arWrk;
	List<object> listWrk;
	Func<string> lookupFilter;
	#pragma warning restore 168
}
<script>
var fLeadStatusadd, currentPageID;
loadjs.ready("head", function() {

	// Form object
	currentPageID = ew.PAGE_ID = "add";
	fLeadStatusadd = currentForm = new ew.Form("fLeadStatusadd", "add");

	// Validate form
	fLeadStatusadd.validate = function() {
		if (!this.validateRequired)
			return true; // Ignore validation
		var $ = jQuery, fobj = this.getForm(), $fobj = $(fobj);
		if ($fobj.find("#confirm").val() == "confirm")
			return true;
		var elm, felm, uelm, addcnt = 0;
		var $k = $fobj.find("#" + this.formKeyCountName); // Get key_count
		var rowcnt = ($k[0]) ? parseInt($k.val(), 10) : 1;
		var startcnt = (rowcnt == 0) ? 0 : 1; // Check rowcnt == 0 => Inline-Add
		var gridinsert = ["insert", "gridinsert"].includes($fobj.find("#action").val()) && $k[0];
		for (var i = startcnt; i <= rowcnt; i++) {
			var infix = ($k[0]) ? String(i) : "";
			$fobj.data("rowindex", infix);
			@if (LeadStatus_Add.Id.Required) {
				<text>
				elm = this.getElements("x" + infix + "_Id");
				if (elm && !ew.isHidden(elm) && !ew.hasValue(elm))
					return this.onError(elm, "@Html.Raw(JsEncode(Convert.ToString(LeadStatus_Add.Id.RequiredErrorMessage).Replace("%s", LeadStatus_Add.Id.Caption)))");
				</text>
			}
				elm = this.getElements("x" + infix + "_Id");
				if (elm && !ew.checkInteger(elm.value))
					return this.onError(elm, "@(JsEncode(LeadStatus_Add.Id.ErrorMessage))");
			@if (LeadStatus_Add.DisplayName.Required) {
				<text>
				elm = this.getElements("x" + infix + "_DisplayName");
				if (elm && !ew.isHidden(elm) && !ew.hasValue(elm))
					return this.onError(elm, "@Html.Raw(JsEncode(Convert.ToString(LeadStatus_Add.DisplayName.RequiredErrorMessage).Replace("%s", LeadStatus_Add.DisplayName.Caption)))");
				</text>
			}

				// Call Form_CustomValidate event
				if (!this.Form_CustomValidate(fobj))
					return false;
		}

		// Process detail forms
		var dfs = $fobj.find("input[name='detailpage']").get();
		for (var i = 0; i < dfs.length; i++) {
			var df = dfs[i], val = df.value;
			if (val && ew.forms[val])
				if (!ew.forms[val].validate())
					return false;
		}
		return true;
	}

	// Form_CustomValidate
	fLeadStatusadd.Form_CustomValidate = function(fobj) { // DO NOT CHANGE THIS LINE!

		// Your custom validation code here, return false if invalid.
		return true;
	}

	// Use JavaScript validation or not
	fLeadStatusadd.validateRequired = @(Config.ClientValidate ? "true" : "false");

	// Dynamic selection lists
	loadjs.done("fLeadStatusadd");
});
</script>
<script>
loadjs.ready("head", function() {

	// Client script
	// Write your client script here, no need to add script tags.

});
</script>
@LeadStatus_Add.ShowPageHeader()
@LeadStatus_Add.ShowMessages()
<form name="fLeadStatusadd" id="fLeadStatusadd" class="@(LeadStatus_Add.FormClassName)" action="@AppPath(CurrentPageName())" method="post">
@if (LeadStatus_Add.CheckToken) {
	@Html.AntiForgeryToken()
}
<input type="hidden" name="action" id="action" value="insert">
<input type="hidden" name="modal" value="@ConvertToInt(LeadStatus_Add.IsModal)">
<div class="ew-add-div"><!-- page* -->
@if (LeadStatus_Add.Id.Visible) { // LeadStatus_Add.Id.Visible (begin)
	<div id="r_Id" class="form-group row">
		<label id="elh_LeadStatus_Id" for="x_Id" class="@(LeadStatus_Add.LeftColumnClass)">@Html.Raw(LeadStatus_Add.Id.Caption)@Html.Raw(LeadStatus_Add.Id.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
		<div class="@(LeadStatus_Add.RightColumnClass)"><div @Html.Raw(LeadStatus_Add.Id.CellAttributes)>
<span id="el_LeadStatus_Id">
<input type="text" data-table="LeadStatus" data-field="x_Id" name="x_Id" id="x_Id" size="30" maxlength="4" placeholder="@Html.Raw(HtmlEncode(LeadStatus_Add.Id.PlaceHolder))" value="@(LeadStatus_Add.Id.EditValue)" @Html.Raw(LeadStatus_Add.Id.EditAttributes)>
</span>
@Html.Raw(LeadStatus_Add.Id.CustomMsg)</div></div>
	</div>
} @* LeadStatus_Add.Id.Visible (end) *@
@if (LeadStatus_Add.DisplayName.Visible) { // LeadStatus_Add.DisplayName.Visible (begin)
	<div id="r_DisplayName" class="form-group row">
		<label id="elh_LeadStatus_DisplayName" for="x_DisplayName" class="@(LeadStatus_Add.LeftColumnClass)">@Html.Raw(LeadStatus_Add.DisplayName.Caption)@Html.Raw(LeadStatus_Add.DisplayName.Required ? Language.Phrase("FieldRequiredIndicator") : "")</label>
		<div class="@(LeadStatus_Add.RightColumnClass)"><div @Html.Raw(LeadStatus_Add.DisplayName.CellAttributes)>
<span id="el_LeadStatus_DisplayName">
<input type="text" data-table="LeadStatus" data-field="x_DisplayName" name="x_DisplayName" id="x_DisplayName" size="30" maxlength="50" placeholder="@Html.Raw(HtmlEncode(LeadStatus_Add.DisplayName.PlaceHolder))" value="@(LeadStatus_Add.DisplayName.EditValue)" @Html.Raw(LeadStatus_Add.DisplayName.EditAttributes)>
</span>
@Html.Raw(LeadStatus_Add.DisplayName.CustomMsg)</div></div>
	</div>
} @* LeadStatus_Add.DisplayName.Visible (end) *@
</div><!-- /page* -->
@{ LeadStatus_Add.CloseRecordset(); }
@if (!LeadStatus_Add.IsModal) {
@:<div class="form-group row"><!-- buttons .form-group -->
	@:<div class="@(LeadStatus_Add.OffsetColumnClass)"><!-- buttons offset -->
<button class="btn btn-primary ew-btn" name="btn-action" id="btn-action" type="submit">@Html.Raw(Language.Phrase("AddBtn"))</button>
<button class="btn btn-default ew-btn" name="btn-cancel" id="btn-cancel" type="button" data-href="@AppPath(LeadStatus_Add.ReturnUrl)"> @Html.Raw(Language.Phrase("CancelBtn"))</button>
	@:</div><!-- /buttons offset -->
@:</div><!-- /buttons .form-group -->
}
</form>
@LeadStatus_Add.ShowPageFooter()
@ShowDebugMessage()
@ElapsedTime()
<script>
loadjs.ready("load", function() {

	// Startup script
	// Write your table-specific startup script here
	// document.write("page loaded");

});
</script>